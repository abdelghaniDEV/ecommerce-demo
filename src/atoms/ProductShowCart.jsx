import { atom } from "recoil";


export const productShowCartsatate = atom({
    key: 'productShowCartsatate', // unique ID (with respect to other atoms/selectors)
    default: [], // default value (aka initial value)
  });